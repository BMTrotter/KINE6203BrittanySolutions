function [Answer] = FindPrime(Number)
%FindPrime will specify whether a number is prime
%   User inputs a number and the function will output a logical value sp

if (isprime(Number))
    Answer = true
else
    Answer = false
end

